---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
       packages:
         - zsh
         - zsh-autosuggestions
         - zsh-syntax-highlighting
         - starship
         - fastfetch
         - vim
         - zoxide
         - fd-find
         - bat
         - fzf
         - distrobox
         - podman-compose
         - git
         - nodejs
         - pipx
         - python3-pip