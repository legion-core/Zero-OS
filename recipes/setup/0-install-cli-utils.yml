---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

###############
### Modules ###
###############
modules:
  - type: dnf
    install:
       install-weak-deps: false
       packages:
         - zsh
         - zsh-autosuggestions
         - zsh-syntax-highlighting
         - starship
         - fastfetch
         - vim
         - zoxide
         - fd-find
         - bat
         - fzf
         - distrobox
         - podman-compose
         - git
         - nodejs
         - pipx
         - python3-pip
         - python3-dotenv+cli
         - nodejs-npm
         - nodejs-full-i18n
#################################################################################################################
### Installs additional shell and packages (most used ones) that improve terminal usability + basic dev tools ###
#################################################################################################################