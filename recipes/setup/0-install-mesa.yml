---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

###############
### Modules ###
###############
modules:
 - type: dnf
   install:
     install-weak-deps: false
     packages:
      - mesa-libGLU
      - mesa-vulkan-drivers
      - vulkan-tools
      - libva-utils
      - intel-media-driver
      - intel-vpl-gpu-rt
      - mesa-va-drivers-freeworld
      - mesa-vdpau-drivers-freeworld
#########################################################################
### Installs AMD/Intel GPU drivers ( rcom is not included by default) ###
#########################################################################