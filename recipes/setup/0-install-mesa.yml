---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

###############
### Modules ###
###############
modules:
 - type: dnf
   install:
     install-weak-deps: false
     packages:
      - mesa-libGLU
      - vulkan-tools
      - libva-utils
########################################################################################################
### Installs mesa GPU drivers ( some libs are not listed here but they are included in final image ) ###
########################################################################################################