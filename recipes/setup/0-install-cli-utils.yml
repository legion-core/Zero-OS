---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

###############
### Modules ###
###############
modules:
  - type: dnf
    install:
       install-weak-deps: false
       packages:
         - zsh
         - zsh-autosuggestions
         - zsh-syntax-highlighting
         - vim
         - fzf
#############################################################################################################
### Installs additional shell and packages (most used ones) that improve terminal usability + basic tools ###
#############################################################################################################