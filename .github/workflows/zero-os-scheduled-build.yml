name: Zero-OS (scheduled build images)

on:
  ### Builds are managed by cron-job.org ( much more reliable ) ###
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  bluebuild:
    name: Zero-OS Scheduled Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        recipe:
          - 0-mesa.yml
          - 0-nvidia.yml
          - 0-mini.yml
          - 0-nvidia-hybrid.yml

    steps:
      - name: Announce scheduled build
        run: |
          echo "=========================================="
          echo " Zero-OS Scheduled Build Starting"
          echo " Triggered via workflow_dispatch"
          echo "=========================================="

      - name: Zero-OS build/update (scheduled)
        uses: blue-build/github-action@v1.11
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ""
          verify_install: true
          maximize_build_space: true
